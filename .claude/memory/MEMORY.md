# Project Memory — Swim Gala Timing System

- [User is UK-based](user-uk-based.md) — UK suppliers (Amazon.co.uk/RS/CPC), 230V/50Hz + BS plugs, 12V sirens, Swim England + Sport Systems, price in £
- [Export = Sport Systems Dolphin file](export-do4-sportsystems.md) — ESP32 → **.do3** per heat into Sport Systems' CTS Dolphin folder → Lenex → Swim England; keep Sport Systems, don't build a direct Lenex exporter (binary analysis: SS reads .do3 not .do4; see SPORTSYSTEMS.md / SPORTSYSTEMS-AOE-NOTES.md)

- [BLE capture bottleneck](ble-capture-bottleneck.md) — 85-89% test likely human artifacts not radio drops; ESP32-C5 gateway now built & stable (firmware/esp32_shelly_scanner), streams PRESS lines over USB serial
- [Server.ts BUILT](server-ts-not-built.md) — src/server.ts works (npm start, port 8000): ESP32 over serial → race state machine → serves static/ dashboard + /remote over WS; do3/lif export. Matches app.js protocol (server.py was stale). View in VS Code Simple Browser; check lsof :8000 before re-launch (EADDRINUSE)
- [Results publishing](results-publishing.md) — completed heats → SQLite (results.db) → single-file static public/results.html (npm run publish, /results); swimmer+event details imported via roster.csv/events.csv. Dashboard re-enroll button (Settings). Keep frontend simple (no RTK/React)
- [Gateway Wi-Fi & flashing](gateway-wifi-flashing.md) — flash ESP32-C5 with arduino-cli FQBN esp32c5 + PartitionScheme=huge_app (default too small); use 2.4GHz SSID only (5GHz invisible to scan); swim-timer.local:3333; dashboard Wi-Fi dot + 10s status re-emit; serial port now auto-detects + auto-reconnects on replug (DEFAULT_PORT=auto)
- [BLE/Wi-Fi scan-window ratio](ble-wifi-scan-window-ratio.md) — coexistence tuned by BLE scan window/interval duty in firmware; **75/25 chosen** (solid + max BLE), 80/20 flaps, 70/30 safe fallback; verify with ping swim-timer.local
- [Node mDNS needs family:4](node-mdns-local-needs-family4.md) — server↔gateway TCP connect MUST force `family:4` or the IPv6/.local lookup hangs & dashboard never connects (transport:null while ping works); link is now Wi-Fi-first/serial-fallback, dashboard Link chip shows active transport
- [wifi=unknown is a serial-transport artifact](wifi-unknown-is-serial-transport-artifact.md) — dashboard wifi "unknown" just means the server is on USB-serial (no WIFI lines), NOT that the gateway Wi-Fi is down — verify with ping swim-timer.local
- [Empty-lane NT workflow](empty-lane-nt-workflow.md) — no-swimmer/unfinished lanes handled by manual Stop → NT (already works); roster-based auto-exclude was considered and DROPPED, don't re-propose
- [SPORTSYSTEMS .do3 format + RE](sportsystems-do3-format.md) — SS app is **VB6** (installer is Inno/Delphi — don't confuse); .do3 line1 = `event;heat;num_splits;round`; reads from C:\CTSDolphin by race number; filename `{meet}-{event}-{heat}{round}{race}.do3`; round letter F-vs-H unresolved → settle with ONE test capture, don't re-RE the parser
- [Actual board pinout](actual-board-pinout.md) — user's real board (photos) = ESP32-C5-KITC-A V1.2; PCM5102 DAC route on breadboard; physical header rows + which row each pin sits on — draw wiring against THIS, not an invented schematic
- [Sport Systems CrossOver setup](sportsystems-crossover-setup.md) — SS Meet Org 5.3 runs via **CrossOver bottle `sports732`** (NOT the Parallels VM); server can write .do3 straight into the bottle's drive_c; **NumDolphinTimers=0 = SS not configured for Dolphin capture = likely the real blocker**; setup steps + needs ONE live capture
- [Audio chain working](audio-chain-debug.md) — PCM5102→amp/PA audio fixed; root causes were wiring/SCK + speaker-not-powered + keepalive hum (set KEEPALIVE_AMP=0) + ground loop (isolator); firmware has TEST/TESTOFF/TONE + dashboard Test-tone button
